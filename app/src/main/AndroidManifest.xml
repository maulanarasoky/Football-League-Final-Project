<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballleaguefinalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.team.player.DetailsPlayer"
            android:theme="@style/DetailMatch"/>
        <activity
            android:name=".activity.team.DetailsTeamActivity"
            android:theme="@style/DetailMatch" />
        <activity
            android:name=".activity.match.favorite.DetailsFavoriteMatchActivity"
            android:theme="@style/DetailMatch" />
        <activity
            android:name=".activity.match.search.SearchMatchActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.match.DetailsMatchActivity"
            android:theme="@style/DetailMatch" />
        <activity
            android:name=".activity.league.DetailsLeagueActivity"
            android:theme="@style/DetailMatch" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>